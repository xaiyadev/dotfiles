# Format and lint files

when:
  - event: ["push", "manual"]
  - event: ["pull_request"]

steps:
  - name: "Enter development environment"
    command: "nix develop ."
  - name: "run through nix checks"
    command: "nix check"