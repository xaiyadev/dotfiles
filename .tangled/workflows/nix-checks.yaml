# run through nix checks

when:
  - event: ["push", "manual"]
    branch: [ "*" ]
  - event: ["pull_request"]
    branch: [ "*" ]

engine: "nixery"

steps:
  - name: "Enter development environment"
    command: "nix develop ."
  - name: "run through nix checks"
    command: "nix check"