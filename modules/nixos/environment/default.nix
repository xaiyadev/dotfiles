{
  imports = [
    ./documentation.nix # Disable all documentations because we dnt need them
    ./fonts.nix # configure all needed fonts
    ./locale.nix # Localized configuration (region; keyboard-layout; etc.)
  ];
}
