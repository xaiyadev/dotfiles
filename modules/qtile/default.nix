{
    services.xserver = {
        enable = true;

        displayManager.sddm.enable = true;
        windowManager.qtile.enable = true;

        xkb = {
            layout = "de";
            variant = "";
        };

    };
}
